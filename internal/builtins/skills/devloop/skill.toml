# Built-in skill: devloop — RENAMED to devlog.
#
# The dev-workflow skill (diary, devlog dir bootstrap, scratch volume) is now
# named "devlog", matching the .byre-devlog/ dir it curates — the old
# devloop-vs-devlog near-name pair confused the people using it. Same rename
# session, 2026-07-12; the review loop had already moved to the separate
# `codereview` skill.
#
# This stub stays so a config naming "devloop" still resolves — it contributes
# NOTHING (no context, no hooks, no volumes: no diary conventions, no
# .byre-devlog/ bootstrap, and the scratch volume is not mounted — its data is
# untouched and remounts when the box switches). Edit the config's `skills`
# list: devloop -> devlog. The scratch volume's name ("scratch") is unchanged,
# so a renamed box picks its data straight back up.

description = "RENAMED to devlog — swap this entry for \"devlog\" in your skills list (same skill: diary, devlog dir, scratch volume; this stub contributes nothing)."
