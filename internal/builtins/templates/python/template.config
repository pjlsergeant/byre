# Built-in starter template: Python.
base = "python:3.12-bookworm"
apt = ["build-essential"]

# OFFERED, not open (ADR 0020): the PyPI hosts, one press from open in
# `byre config` -> Egress.
egress_offered = ["pypi.org", "files.pythonhosted.org"]
